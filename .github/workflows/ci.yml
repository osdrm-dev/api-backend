name: CI - OSDRM REST API

on:
  pull_request:
    branches:
      - develop
      - main
  push:
    branches:
      - develop

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'

      - name: Install backend dependencies
        run: npm install

      # - name: Run lint
      #   run: npm run lint

      # - name: Run tests
      #   run: npm run test

      - name: Build application
        run: npm run build
